eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      default-zone: http://${eureka.instance.hostname}:8761/eureka/

management:
  security:
    enabled: false
spring:
  cloud:
    config:
      server:
        git:
          default-label:
          uri: http://git@github.com:Humphrey-Peng/config-server.git



